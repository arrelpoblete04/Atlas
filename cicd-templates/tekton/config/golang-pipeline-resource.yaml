apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: golang-pipeline-resource
spec:
  type: git
  params:
    - name: url
      value: https://github.com/arrelpoblete04/GolangBackend #URL of repository.
    - name: revision
      value: master