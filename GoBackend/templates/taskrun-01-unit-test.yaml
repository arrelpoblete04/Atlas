apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: golang-taskrun-unit-test-golang
spec:
  taskRef:
    name: golang-task-one-unit-test
  resources:
    inputs:
      - name: repo
        resourceRef:
          name: golang-pipeline-resource